<h1>All articles</h1>

<% @articles_list.each do |name|%>
<h2><%= name.title %> </h2>
  <p><%= name.text %></p>
  <p>
    <%= link_to 'Show', article_path(name) %> |
    <%= link_to 'Edit', edit_article_path(name) %> |
     <!--"мой вариант ссылки на удаление articles"-->
    <!--<%= link_to 'destroy', { action: :destroy, id: name.id }, method: :delete, data: { confirm: 'Are you sure?' } %>-->
         <!--"второй вариант удаления"-->
    <%= link_to 'Destroy', article_path(name), method: :delete, data: {confirm: 'Are you sure?'} %>


    <!--<a href="/articles/<%= name.id %>" >Show</a>-->
  </p>
  <hr/>
 <% end %>